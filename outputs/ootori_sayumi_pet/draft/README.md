# 客户端试装草稿

此图集从已有旧草稿做确定性修复：补入 neutral 默认帧并清零完全透明位置的 RGB。已有可见像素保持不变，使用无损 WebP。

`structure-check.json` 仅确认格式、尺寸、空格、neutral 和像素保留；不表示完整视觉验收通过。静态重复帧、半身截断、气泡、贴纸轮廓和错误的视线姿势仍需重做。

最新透明抠图请查看 `../cutouts-v4/preview.html`。安装命令为项目根目录的 `.\install-pet.ps1`，已有安装会自动备份。
